<section class="hero-block markdown-content">
  {{{renderMarkdown page.markdown}}}
</section>

<section class="panel">
  <h3>Top 10 — Overall Rankings</h3>
  <p class="eyebrow">Ranked by combined score across all games</p>
  <table>
    <thead>
      <tr>
        <th>Rank</th>
        <th>Player</th>
        <th>Total Score</th>
      </tr>
    </thead>
    <tbody>
      {{#each leaderboard}}
        <tr>
          <td>
            {{#if @first}}
              <span class="rank-badge gold">#{{rank}}</span>
            {{else}}
              <span class="rank-badge">#{{rank}}</span>
            {{/if}}
          </td>
          <td><a href="/users/{{userId}}">{{name}}</a></td>
          <td>{{score}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</section>
