services:
  leaderboard-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - leaderboard_data:/app/data

volumes:
  leaderboard_data:
